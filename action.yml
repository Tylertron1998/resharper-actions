name: 'Resharper Actions'
description: 'Runs Resharper configurably on CI.'
author: 'Tylertron'
inputs:
  SLN_DIR:
    description: 'path to the .sln file'
    default: '.'
  FAIL_LEVEL:
    description: 'Level at which CI fails.'
    default: 'WARNING'
runs:
  using: 'node13.3'
  main: 'src/index.js --harmony-top-level-await'